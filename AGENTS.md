# AGENTS.md

本文件定义在本仓库中协作的 Agent / AI 工具的统一工作约束。

## 1. 首要目标

本项目当前阶段的首要目标是：

- 以**维护性优先**的方式，构建一个结构清晰的大模型浏览器增强 Agent。
- 当前从基础聊天能力起步，在不破坏结构边界的前提下，逐步扩展 provider、tools 与浏览器增强能力。

如无特别说明，所有结构调整、代码实现、重构决策都应优先服从“维护性优先”。

## 2. 必读规范

开始任何结构性任务前，必须先阅读：

- `docs/superpowers/specs/2026-04-04-browser-agent-project-spec.md`

并且在执行任何回复、澄清、设计、实现、调研之前，必须先检查并加载skill，再继续后续动作。禁止跳过 skill 检查直接开始分析、提问或实现。

该文件是本项目当前阶段的主规范，定义了：

- 目标目录结构
- 各目录职责边界
- 命名规范
- 依赖方向
- 当前阶段保留范围与不做范围
- 后续 AI 协作要求

## 3. 如何引用 spec

在执行任务时，Agent 应把 spec 作为结构性判断依据，而不是仅作为背景描述。

### 3.1 遇到以下任务时，必须显式参考 spec

- 新增文件或移动文件
- 拆分大文件
- 调整模块边界
- 增加 `providers` / `tools` / `background` / `ui` 相关能力
- 修改命名规范
- 恢复曾经被暂时移除的功能

### 3.2 若实现与 spec 冲突

默认处理顺序：

1. 优先保持结构边界稳定。
2. 优先维护性，而不是短期交付便利。
3. 若需求必须突破现有 spec，应先更新 spec，再修改代码。

## 4. 目录归属判断规则

新增代码前，先判断归属：

- 用户可见界面 → `src/ui`
- Chrome 后台任务、消息路由、调度 → `src/background`
- LLM provider、stream、tool 协议 → `src/llm`
- 跨层通用能力 → `src/shared`

如果一个模块同时像 `ui` 又像 `background`，说明职责拆分还不够，应继续拆分。

## 5. 当前硬性边界

- `ui` 不直接依赖 provider 实现。
- `ui` 不直接承担 LLM 编排与 tool loop。
- `background` 不直接书写 JSX。
- `llm` 不直接依赖 Chrome API。
- `shared` 不放业务特定逻辑。
- `index.ts` 仅用于入口装配或导出聚合，不承载主要业务逻辑。

## 6. 当前阶段的功能策略

当前阶段仅保留：

- 基础聊天能力
- 最小设置能力
- provider 抽象
- tools 接口边界

当前阶段不应重新引入：

- 语音识别 / 同声传译
- 图片分析 / 截图分析
- PDF / 滚动捕获
- 网络录制 / 页面流量分析
- 任何具体工具实现

如果任务要求恢复上述能力，必须先确认其在 spec 中的归属与边界。

## 7. 命名要求

默认遵循 spec 中的命名规范：

- 目录名使用 `kebab-case`
- React 组件文件使用 `PascalCase.tsx`
- 非组件文件使用 `kebab-case.ts`
- 组件名使用 `PascalCase`
- 普通函数使用 `camelCase`
- Hook 使用 `useXxx`
- 职责型文件优先使用显式后缀，例如 `*-provider.ts`、`*-orchestrator.ts`、`*-repository.ts`

不要引入模糊命名，如：

- `utils2.ts`
- `commonService.ts`
- `temp.ts`
- `AppHelpers.ts`

## 8. 变更原则

- 先拆边界，再写实现。
- 先删废弃调用链，再删旧实现。
- 不要为了省事把逻辑塞回超级文件。
- 若文件已经承担多种职责，应优先拆分。
- 不要把“暂时没地方放”的代码扔进 `shared`。
- 在满足需求的前提下，优先采用**最小可行改动**，避免一次性大范围重构或大面积改写。
- 永远不要仅因为测试失败而修改生产 / 主逻辑。测试用例服务于当前主逻辑与已确认需求；如果失败来自测试断言过期、测试替身不符合主逻辑实际调用方式，或测试覆盖了本轮无关 / 未确认行为，应优先调整测试用例或先确认需求，而不是为了让测试通过去改主逻辑。只有当用户需求或已确认缺陷明确要求改变行为时，才修改主逻辑。
- 如确需进行**大范围改动**（涉及多文件、多模块或核心流程重构），必须先输出设计与规划，列出：
  - 受影响的文件清单；
  - 每个文件的预期改动范围
  在得到确认后再开始具体实现。
- 只允许修改工作区文件，不允许创建任何 git commit（包括本地 commit）。Agent 最多只能查看 git 提交记录、状态或 diff 等只读信息。

## 9. 文档维护要求

当以下内容发生变化时，应同步更新 spec：

- 一级目录结构变化
- 模块职责变化
- 命名规范变化
- 本轮保留范围变化
- AI 协作边界变化

除此之外：

- 每次完成一项功能开发或较大的交互改动后，Agent 需要主动检查本轮改动是否影响目录职责、依赖边界或协作约定；如有影响，应同步更新 `docs/superpowers/specs` 下的项目 spec，使文档与实现保持一致。

- UI 交互、布局和状态相关的设计图统一位于 `design/` 目录（例如 `design/1.png` 等文件）。

如果只是小型实现变更，不必更新 spec；如果是结构性变化或对外行为变化，必须更新。

## 10. 决策默认值

若任务描述不完整，默认按以下顺序决策：

1. 选择更容易维护的方案。
2. 选择边界更清晰的方案。
3. 选择更小、更可回滚的改动。
4. 不提前实现未进入本轮范围的功能。

## 11. 给未来 Agent 的一句话

这个项目当前不是在追求“最快堆出最多功能”，而是在追求“最容易长期维护的大模型浏览器增强 Agent 骨架”。

如果某项实现会让项目再次回到“大文件混合 UI / background / LLM / tools”的状态，那它大概率就是错误方向。
